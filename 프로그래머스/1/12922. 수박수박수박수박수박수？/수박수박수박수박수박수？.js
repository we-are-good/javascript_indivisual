function solution(n) {
    var half = parseInt(n/2);
    var low = n%2;
    var answer = '';
    for (i=0; i<half; i++){
            answer += "수박"}
    low === 0 ? answer : answer+="수"    
    return answer;
}